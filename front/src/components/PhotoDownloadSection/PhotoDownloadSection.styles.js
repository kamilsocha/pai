import styled from '@emotion/styled'

const Container = styled.div`
  flex: 1;
  display: flex;
  overflow-x: auto;
  padding: 4px;
  border: 4px solid gray;
  > * {
    margin: 0 8px;
  }
`

export { Container }
